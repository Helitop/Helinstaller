<Page x:Class="Helinstaller.Views.Pages.DownloadsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:helpers="clr-namespace:Helinstaller.Helpers"
      xmlns:viewmodels="clr-namespace:Helinstaller.ViewModels.Pages"
      d:DataContext="{d:DesignInstance viewmodels:DownloadsViewModel}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="DownloadsPage"
          ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <helpers:StringToImageSourceConverter x:Key="ImgConv"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid Margin="24,15,24,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel LastChildFill="False" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <ui:TextBlock Text="Загрузки" FontSize="28" FontWeight="Bold"/>
                <ui:TextBlock Text="Список текущих и завершенных установок" Foreground="Gray"/>
            </StackPanel>
            <ui:Button DockPanel.Dock="Right" Content="Очистить всё" Icon="{ui:SymbolIcon Broom24}" 
                       Command="{Binding ClearHistoryCommand}" Appearance="Secondary" VerticalAlignment="Bottom"/>
        </DockPanel>

        <ui:ListView Grid.Row="1" 
             ItemsSource="{Binding DownloadTasks}" 
             SelectionMode="Single">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <ui:Card Margin="0,4" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" CornerRadius="6" Background="#20FFFFFF">
                                <Image Source="{Binding IconPath, Converter={StaticResource ImgConv}}" Margin="4"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0">
                                <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <ProgressBar Value="{Binding Progress}" IsIndeterminate="{Binding IsIndeterminate}" Height="4"
                                               Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>
                                <TextBlock Text="Завершено" Foreground="LightGreen" HorizontalAlignment="Center" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                            <ui:Button Grid.Column="3" Icon="{ui:SymbolIcon Dismiss24}" Appearance="Transparent" 
                                       Command="{Binding DataContext.RemoveTaskCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}"/>
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</Page>