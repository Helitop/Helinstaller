<Page
    x:Class="Helinstaller.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d" Margin="0,0,0,0" Loaded="Page_Loaded" 
    Title="Dashboard"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <!-- Анимации перехода на страницу приложения -->
        <Storyboard x:Key="FadeInBg">
            <DoubleAnimation Storyboard.TargetName="DashboardBackground" Storyboard.TargetProperty="Opacity" To="0.15" Duration="0:0:0.6"/>
            <DoubleAnimation Storyboard.TargetName="BackgroundDimmer" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.6"/>
        </Storyboard>
        <Storyboard x:Key="FadeOutBg">
            <DoubleAnimation Storyboard.TargetName="DashboardBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="BackgroundDimmer" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeInLoading">
            <DoubleAnimation Storyboard.TargetName="LoadingOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.1" />
        </Storyboard>

        <!-- Анимации режима редактирования -->
        <Storyboard x:Key="ShakeAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="-1" To="1" Duration="0:0:0.12" />
        </Storyboard>

        <Storyboard x:Key="SlideDownAnimation">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-40" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="5"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>

        <Storyboard x:Key="SlideUpAnimation">
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="-80" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseIn" Exponent="5"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Visibility" BeginTime="0:0:0.3">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}"/>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <!-- Размытый фон (появляется при клике на приложение) -->
        <Image x:Name="DashboardBackground" Stretch="UniformToFill" Opacity="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image.Effect>
                <BlurEffect Radius="60" KernelType="Gaussian"/>
            </Image.Effect>
        </Image>
        <Border x:Name="BackgroundDimmer" Background="Black" Opacity="0" IsHitTestVisible="False"/>

        <Grid x:Name="AppsGrid" Background="Transparent" MouseLeftButtonDown="Grid_MouseLeftButtonDown" AllowDrop="True" PreviewDragOver="Grid_PreviewDragOver">

            <ui:DynamicScrollViewer x:Name="AppsScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <!-- WrapPanel автоматически переносит плитки. 4 штуки в ряд встанут идеально -->
                <WrapPanel x:Name="AppsWrapPanel" Margin="0,25,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" Orientation="Horizontal" />
            </ui:DynamicScrollViewer>

            <!-- Панель управления порядком (появляется при DND) -->
            <StackPanel x:Name="EditModePanel" Panel.ZIndex="1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,15,0,0" Visibility="Collapsed" Opacity="0">
                <StackPanel.RenderTransform>
                    <TranslateTransform Y="-80"/>
                </StackPanel.RenderTransform>
                <ui:Button Content="Сохранить порядок" Icon="{ui:SymbolIcon Symbol=Checkmark24}" Appearance="Primary" Click="ConfirmChanges_Click" Height="45" Padding="20,0">
                    <ui:Button.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.4"/>
                    </ui:Button.Effect>
                </ui:Button>
            </StackPanel>
        </Grid>

        <!-- Оверлей загрузки -->
        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Panel.ZIndex="100" Opacity="0" Background="#80000000">
            <ui:ProgressRing IsIndeterminate="True" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>