<Page
    x:Class="Helinstaller.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d" Margin="0,0,0,0" Loaded="Page_Loaded" 
    Title="Dashboard"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <!-- Анимации перехода на страницу приложения -->
        <Storyboard x:Key="FadeInBg">
            <DoubleAnimation Storyboard.TargetName="DashboardBackground" Storyboard.TargetProperty="Opacity" To="0.15" Duration="0:0:0.6"/>
            <DoubleAnimation Storyboard.TargetName="BackgroundDimmer" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.6"/>
        </Storyboard>
        <Storyboard x:Key="FadeOutBg">
            <DoubleAnimation Storyboard.TargetName="DashboardBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="BackgroundDimmer" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeInLoading">
            <DoubleAnimation Storyboard.TargetName="LoadingOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.1" />
        </Storyboard>

        <!-- Анимации режима редактирования -->
        <Storyboard x:Key="ShakeAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="-1" To="1" Duration="0:0:0.12" />
        </Storyboard>

        <Storyboard x:Key="SlideDownAnimation">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-40" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="5"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>

        <Storyboard x:Key="SlideUpAnimation">
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="-80" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseIn" Exponent="5"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="EditModePanel" Storyboard.TargetProperty="Visibility" BeginTime="0:0:0.3">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}"/>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <!-- Размытый фон (он должен быть самым нижним слоем) -->
        <Image x:Name="DashboardBackground" Stretch="UniformToFill" Opacity="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image.Effect>
                <BlurEffect Radius="60" KernelType="Gaussian"/>
            </Image.Effect>
        </Image>
        <Border x:Name="BackgroundDimmer" Background="Black" Opacity="0" IsHitTestVisible="False"/>

        <!-- Основная область с контентом -->
        <Grid x:Name="AppsGrid" Background="Transparent" AllowDrop="True" PreviewDragOver="Grid_PreviewDragOver">

                <StackPanel Margin="0,25,0,25">


                    <StackPanel MaxWidth="900" HorizontalAlignment="Stretch" Margin="20,0">
                        <TextBlock Text="Поиск приложений в WinGet" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />

                        <!-- Строка поиска -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                                
                            <ui:TextBox x:Name="SearchTextBox" PlaceholderText="Введите название приложения (например, Telegram, Discord...)" 
                                Icon="{ui:SymbolIcon Symbol=Search24}" 
                                KeyDown="SearchTextBox_KeyDown"
                                Height="40" FontSize="14"/>

                            <ui:Button Grid.Column="1" Content="Найти" Appearance="Primary" 
                               Icon="{ui:SymbolIcon Symbol=Search24}" 
                               Margin="10,0,0,0" Height="40" Padding="20,0"
                               Click="SearchWinGetButton_Click"/>
                        </Grid>

                        <!-- Индикатор загрузки поиска -->
                        <ui:ProgressRing x:Name="WinGetSearchProgress" IsIndeterminate="True" Visibility="Collapsed" Margin="0,20,0,20"/>

                        <!-- Список результатов -->
                    <ui:DynamicScrollViewer MaxHeight="275" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,20">
                        <ItemsControl x:Name="WinGetResultsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ui:Card Margin="0,0,0,10" Padding="15">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" />
                                                <TextBlock Text="{Binding Id}" FontSize="12" Opacity="0.6"/>
                                                <TextBlock Text="{Binding Version}" FontSize="11" Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">

                                                <!-- Индикатор работы (виден только когда IsProcessing = true) -->
                                                <StackPanel Orientation="Horizontal" Visibility="{Binding ProgressVis}">
                                                    <ui:ProgressRing IsIndeterminate="True" Width="20" Height="20" Margin="0,0,10,0"/>
                                                    <TextBlock Text="Работаю..." VerticalAlignment="Center" Opacity="0.7" FontSize="12"/>
                                                </StackPanel>

                                                <!-- Группа кнопок (скрывается целиком, когда IsProcessing = true) -->
                                                <StackPanel Orientation="Horizontal" Visibility="{Binding ActionButtonsVis}">
                                                    <ui:Button Content="Установить" Appearance="Primary" 
                   Click="InstallApp_Click" Visibility="{Binding InstallBtnVis}"/>

                                                    <ui:Button Content="Обновить" Appearance="Success" Margin="5,0,0,0"
                   Click="UpdateApp_Click" Visibility="{Binding UpdateBtnVis}"/>

                                                    <ui:Button Content="Удалить" Appearance="Danger" Margin="5,0,0,0"
                   Click="UninstallApp_Click" Visibility="{Binding UninstallBtnVis}"/>
                                                </StackPanel>

                                            </StackPanel>
                                            <!-- Добавь это внутри StackPanel или Grid карточки поиска -->
                                            <Grid x:Name="ProgressGrid" Visibility="Collapsed" Margin="0,10,0,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <ui:TextBlock x:Name="StatusLabel" Text="Подготовка..." FontSize="11" Margin="0,0,0,5"/>
                                                <ProgressBar x:Name="InstallProgressBar" Grid.Row="1" Height="6" Minimum="0" Maximum="100" Value="0"/>
                                            </Grid>
                                        </Grid>
                                    </ui:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ui:DynamicScrollViewer>
                </StackPanel>
                <Border Margin="40,0,40,0" Height="1" Background="{DynamicResource ControlStrokeColorDefaultBrush}" Opacity="0.5"/>
                <WrapPanel x:Name="AppsWrapPanel" MouseLeftButtonDown="Grid_MouseLeftButtonDown" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Top" Orientation="Horizontal" />
            </StackPanel>


            <!-- Панель управления порядком (появляется при DND) -->
            <StackPanel x:Name="EditModePanel" Panel.ZIndex="1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,15,0,0" Visibility="Collapsed" Opacity="0">
                <StackPanel.RenderTransform>
                    <TranslateTransform Y="-80"/>
                </StackPanel.RenderTransform>
                <ui:Button Content="Сохранить порядок" Icon="{ui:SymbolIcon Symbol=Checkmark24}" Appearance="Primary" Click="ConfirmChanges_Click" Height="45" Padding="20,0">
                    <ui:Button.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.4"/>
                    </ui:Button.Effect>
                </ui:Button>
            </StackPanel>
        </Grid>

        <!-- Оверлей загрузки -->
        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Panel.ZIndex="100" Opacity="0" Background="#80000000">
            <ui:ProgressRing IsIndeterminate="True" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>