<Page
    x:Class="Helinstaller.Views.Pages.Advices"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d" Title="Advices"
    d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <!-- ScrollViewer — единственный хозяин прокрутки здесь -->
        <ui:DynamicScrollViewer x:Name="MainScroll" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled">

            <ItemsControl ItemsSource="{Binding AdviceItems}" Margin="10,10,10,20">

                <!-- Делаем сетку: 3 колонки. UniformGrid сделает ВСЕ карточки одного размера -->
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" VerticalAlignment="Top"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Margin="6" Padding="18" VerticalAlignment="Stretch">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <!-- Иконка -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- Заголовок -->
                                    <RowDefinition Height="*"/>
                                    <!-- Текст -->
                                </Grid.RowDefinitions>

                                <ui:SymbolIcon Grid.Row="0" 
                                               Symbol="{Binding Icon}" 
                                               FontSize="28" 
                                               Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" 
                                               HorizontalAlignment="Left"/>

                                <TextBlock Grid.Row="1" 
                                           Text="{Binding Title}" 
                                           FontWeight="Bold" 
                                           Margin="0,12,0,6" 
                                           FontSize="14" 
                                           TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="2" 
                                           Text="{Binding Description}" 
                                           TextWrapping="Wrap" 
                                           Opacity="0.7" 
                                           FontSize="12" 
                                           LineHeight="16"
                                           VerticalAlignment="Top"/>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ui:DynamicScrollViewer>
    </Grid>
</Page>