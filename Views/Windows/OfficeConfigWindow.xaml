<ui:FluentWindow
    x:Class="Helinstaller.Views.Windows.OfficeConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    Height="500" Width="400"
    Title="Установка Office"
    ResizeMode="NoResize"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen" Topmost="True">

    <ui:DynamicScrollViewer>
        <StackPanel Margin="20" CanVerticallyScroll="True">
            <ui:TextBlock FontWeight="Bold" Margin="0,0,0,10" Text="Базовый продукт (ru-ru 64-бит)"/>

            <ui:ToggleSwitch Content="Office LTSC Professional Plus 2024" IsChecked="{Binding Configuration.InstallProPlus}" IsEnabled="False" FontWeight="Bold"/>

            <Separator Margin="0,5"/>

            <ui:TextBlock FontWeight="Bold" Margin="0,10,0,5" Text="Включить приложения в установку:"/>
            <ui:ToggleSwitch Content="Word" IsChecked="{Binding Configuration.IncludeWord}" Margin="0,5,0,0"/>
            <ui:ToggleSwitch Content="Excel" IsChecked="{Binding Configuration.IncludeExcel}" Margin="0,5,0,0"/>
            <ui:ToggleSwitch Content="PowerPoint" IsChecked="{Binding Configuration.IncludePowerPoint}" Margin="0,5,0,0"/>
            <ui:ToggleSwitch Content="Outlook" IsChecked="{Binding Configuration.IncludeOutlook}" Margin="0,5,0,0"/>
            <ui:ToggleSwitch Content="Access (создание/редактирование баз данных)" IsChecked="{Binding Configuration.IncludeAccess}" Margin="0,5,0,0"/>

            <Separator Margin="0,5"/>

            <ui:TextBlock FontWeight="Bold" Margin="0,10,0,5" Text="Дополнительные продукты:"/>
            <ui:ToggleSwitch Content="Visio Pro LTSC 2024 (создание векторных схем)" IsChecked="{Binding Configuration.InstallVisio}" Margin="0,5,0,0"/>
            <ui:ToggleSwitch Content="Project Pro LTSC 2024" IsChecked="{Binding Configuration.InstallProject}" Margin="0,5,0,0"/>

            <Separator Margin="0,5"/>
            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <ui:Button Content="Начать установку" Click="InstallButton_Click" Padding="5" HorizontalAlignment="Stretch" Margin="0,0,5,0"/>
                <ui:Button Grid.Column="1" Content="Отмена" Click="CloseButton_click" Padding="5" HorizontalAlignment="Stretch" Margin="5,0,0,0"/>
            </Grid>
        </StackPanel>
    </ui:DynamicScrollViewer>
</ui:FluentWindow>