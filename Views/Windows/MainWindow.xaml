<ui:FluentWindow
    x:Class="Helinstaller.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Helinstaller.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding ViewModel.ApplicationTitle, Mode=OneWay}"
    Width="1100"
    Height="650"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d" ResizeMode="NoResize" MaxHeight="650" MaxWidth="1100" ExtendsContentIntoTitleBar="True" Initialized="FluentWindow_Initialized">
    <Grid>
        <Grid x:Name="MainGrid" Visibility="Hidden" Opacity="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ui:NavigationView
            x:Name="RootNavigation"
            Grid.Row="1"
            Padding="42,0,42,0"
            BreadcrumbBar="{Binding ElementName=BreadcrumbBar}"
            FooterMenuItemsSource="{Binding ViewModel.FooterMenuItems, Mode=OneWay}"
            FrameMargin="0"
            IsBackButtonVisible="Visible"
            IsPaneToggleVisible="True"
            MenuItemsSource="{Binding ViewModel.MenuItems, Mode=OneWay}"
            PaneDisplayMode="Top" TransitionDuration="500">
                <ui:NavigationView.Header>
                    <ui:BreadcrumbBar x:Name="BreadcrumbBar" Margin="42,32,42,20" Visibility="Collapsed"/>
                </ui:NavigationView.Header>
                <ui:NavigationView.ContentOverlay>
                    <Grid x:Name="ContentGrid">
                        <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                    </Grid>
                </ui:NavigationView.ContentOverlay>

            </ui:NavigationView>

            <ContentPresenter
            x:Name="RootContentDialog"
            Grid.Row="0"
            Grid.RowSpan="2" />

            <ui:TitleBar
            x:Name="TitleBar"
            Title="{Binding ViewModel.ApplicationTitle}"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="True" ShowMaximize="False">
                <ui:TitleBar.Icon>
                    <ui:ImageIcon Source="/Assets/H.png" />
                </ui:TitleBar.Icon>
            </ui:TitleBar>
            <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal" FlowDirection="RightToLeft" Grid.Row="1" HorizontalAlignment="Stretch" Background="#4C000000">

                <ui:HyperlinkButton Height="30" FlowDirection="LeftToRight" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" Foreground="White" NavigateUri="https://t.me/hltp325" OverridesDefaultStyle="True">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:TextBlock Text="HLTP"/>
                        <ui:Image HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0,0,0" HorizontalContentAlignment="Right" Width="20" Source="/Assets/tg.png"/>
                    </StackPanel>
                </ui:HyperlinkButton>
                <ui:HyperlinkButton Height="30" FlowDirection="LeftToRight" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" Foreground="White" NavigateUri="https://github.com/Helitop">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:TextBlock Text="Helitop"/>
                        <ui:Image HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0,0,0" HorizontalContentAlignment="Right" Width="20" Source="/Assets/github.png"/>
                    </StackPanel>
                </ui:HyperlinkButton>
                <ui:HyperlinkButton Height="30" FlowDirection="LeftToRight" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" Foreground="White" NavigateUri="https://www.tiktok.com/@helicopter6622">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:TextBlock Text="@helicopter6622"/>
                        <ui:Image HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0,0,0" HorizontalContentAlignment="Right" Width="20" Source="/Assets/tt.png"/>
                    </StackPanel>
                </ui:HyperlinkButton>
                <ui:HyperlinkButton Height="30" FlowDirection="LeftToRight" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" Foreground="White" NavigateUri="https://www.youtube.com/@HelitopAnimation">
                    <StackPanel Orientation="Horizontal">
                        <ui:TextBlock Text="@HelitopAnimation"/>
                        <ui:Image Source="/Assets/yt.png" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0,0,0" HorizontalContentAlignment="Right" Width="20" />
                    </StackPanel>
                </ui:HyperlinkButton>
                <ui:HyperlinkButton 
                    Height="30" 
                    FlowDirection="LeftToRight" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom" 
                    Grid.Row="1" 
                    Foreground="White" 
                    Command="{Binding ViewModel.ToDonationCommand}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:TextBlock Text="Закинуть на сухарики"/>
                        <ui:Image HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0,0,0" HorizontalContentAlignment="Right" Width="20" Source="/Assets/tbank.png" CornerRadius="20"/>
                    </StackPanel>
                </ui:HyperlinkButton>
            </StackPanel>
        </Grid>
        <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center">
            <ui:TextBlock x:Name="desc" Text="Подключение..." FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20"/>
            <ui:SymbolIcon x:Name="stat" MaxHeight="60" MaxWidth="60" Visibility="Collapsed" Symbol="CloudCheckmark48" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static SystemColors.AccentColorBrushKey}}" FontSize="64"/>
            <ui:ProgressRing x:Name="ring" MaxHeight="60" MaxWidth="60" IsIndeterminate="True" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>

</ui:FluentWindow>
