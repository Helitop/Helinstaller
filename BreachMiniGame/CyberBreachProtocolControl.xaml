<UserControl x:Class="Helinstaller.BreachMiniGame.CyberBreachProtocolControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Helinstaller.BreachMiniGame"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>

        <SolidColorBrush x:Key="BackgroundPrimary" Color="#FF121F1F"/>
        <SolidColorBrush x:Key="NeonBlue" Color="#FFCFED58"/>
        <SolidColorBrush x:Key="NeonRed" Color="#FF0ADEAD"/>
        <SolidColorBrush x:Key="AccentGrey" Color="#33334D"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#EEEEEE"/>

        <Style x:Key="MatrixButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource NeonBlue}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentGrey}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1A00FFFF"/>
                                <Setter Property="BorderBrush" Value="{StaticResource NeonBlue}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>

                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource NeonRed}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource NeonRed}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding IsCurrentSelectionRow}" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource NeonBlue}"/>
                                <Setter Property="BorderThickness" Value="3"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding IsCurrentSelectionCol}" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource NeonRed}"/>
                                <Setter Property="BorderThickness" Value="3"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BufferCodeStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource NeonBlue}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="#1A00FFFF"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundPrimary}" Height="450" Width="950">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock FontFamily="Consolas"
            Foreground="{StaticResource NeonBlue}"
           Opacity="0.5"
                   Margin="50,0,0,0"
           FontSize="8"
           TextWrapping="Wrap"
           xml:space="preserve">
<![CDATA[
// Динамическое создание кнопок матрицы
private void BuildMatrixUI()
{
    // Очистка предыдущего состояния
    MatrixGrid.Children.Clear();
    MatrixGrid.RowDefinitions.Clear();
    MatrixGrid.ColumnDefinitions.Clear();

    int size = Game.Matrix.Count;

    // Создание определений строк и столбцов
    for (int i = 0; i < size; i++)
    {
        MatrixGrid.RowDefinitions.Add(new RowDefinition { Height = new GridLength(1, GridUnitType.Star) });
        MatrixGrid.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(1, GridUnitType.Star) });
    }

    // Создание и размещение кнопок
    for (int i = 0; i < size; i++)
    {
        for (int j = 0; j < size; j++)
        {
            var cell = Game.Matrix[i][j];

            var button = new Button
            {
                Content = cell.Code,
                Style = (Style)this.FindResource("MatrixButtonStyle"),
                DataContext = cell // Привязка данных к модели ячейки
            };

            // Установка позиции в Grid
            Grid.SetRow(button, i);
            Grid.SetColumn(button, j);

            // Добавление обработчика клика
            button.Click += MatrixCell_Click;

            MatrixGrid.Children.Add(button);
        }
    }
}
]]>
        </TextBlock>
        <TextBlock FontFamily="Consolas"
                   Grid.Column="1"
            Foreground="{StaticResource NeonBlue}"
           Opacity="0.5"
           FontSize="9"
                   HorizontalAlignment="Right"
           TextWrapping="Wrap"
           xml:space="preserve">
<![CDATA[
            <ItemsControl ItemsSource="{Binding TargetSequences}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{Binding StatusColor}" BorderThickness="2" Margin="0,0,0,10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" 
                                           Foreground="{Binding StatusColor}" FontSize="16" FontWeight="Bold" 
                                           FontFamily="Consolas" Margin="0,0,0,5"/>
                                <ItemsControl ItemsSource="{Binding Codes}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" 
                                                       Foreground="{StaticResource NeonBlue}" FontSize="14" 
                                                       FontFamily="Consolas" Margin="0,0,5,0"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
]]>
        </TextBlock>
        <StackPanel Grid.Column="0" Margin="20">

            <TextBlock Text="{Binding BufferStatus}" 
                       Foreground="{StaticResource NeonRed}" FontSize="24" 
                       FontFamily="Consolas" Margin="0,0,0,10"/>

            <ItemsControl ItemsSource="{Binding Buffer}" Margin="0,0,0,20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Style="{StaticResource BufferCodeStyle}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border BorderBrush="{StaticResource AccentGrey}" BorderThickness="3" Background="#99121F1F" Padding="5">
                <Grid x:Name="MatrixGrid">
                </Grid>
            </Border>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="20,20,20,20">
            <TextBlock Text="ПОСЛЕДОВАТЕЛЬНОСТИ" 
                       Foreground="{StaticResource NeonBlue}" FontSize="20" 
                       FontFamily="Consolas" Margin="0,0,0,10"/>

            <ItemsControl ItemsSource="{Binding TargetSequences}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{Binding StatusColor}" BorderThickness="2" Margin="0,0,0,10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" 
                                           Foreground="{Binding StatusColor}" FontSize="16" FontWeight="Bold" 
                                           FontFamily="Consolas" Margin="0,0,0,5"/>
                                <ItemsControl ItemsSource="{Binding Codes}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" 
                                                       Foreground="{StaticResource NeonBlue}" FontSize="14" 
                                                       FontFamily="Consolas" Margin="0,0,5,0"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Visibility="{Binding GameFinished, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,20,0,0">
                <TextBlock Text="ПРОТОКОЛ ВЗЛОМА ЗАВЕРШЕН" FontSize="18" FontWeight="Bold" FontFamily="Consolas" Margin="0,10"/>
                <TextBlock Text="{Binding GameWon, Converter={StaticResource GameResultConverter}}" FontSize="24" FontWeight="Bold" FontFamily="Consolas" 
                           Foreground="{Binding GameWon, Converter={StaticResource GameResultColorConverter}}"/>
                <Button Content="ПЕРЕЗАГРУЗИТЬ" Click="RestartButton_Click" Margin="0,15,0,0"
                        Background="{StaticResource NeonRed}" Foreground="{StaticResource TextPrimary}"
                        BorderBrush="{StaticResource NeonRed}" BorderThickness="2" Padding="10,5"/>
            </StackPanel>

        </StackPanel>
    </Grid>

</UserControl>